<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <title>FoodFinder</title>
    <style>
        .card {
            height: 100%;
            display: flex;
            flex-direction: column;
            border: 3px solid #3d6e76;
            border-radius: 8px;
        }

        .card-body {
            flex: 1;
            padding: 20px;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
        }

        h2 {
            padding: 20px;
        }

        .card-title {
            text-align: center;
            padding: 10px;
            margin-bottom: 15px;
            color: darkgreen;
        }

        .buttons {
            padding: 5px;
        }

        .card-text img {
            display: block;
            margin: 0 auto;
            max-height: 100px;
            width: auto;
        }

        body {
            background-color: rgb(185, 216, 208);
        }
    </style>
</head>
<body>
    <%- include ('partials/navbar.ejs') %>
    <div class="container mt-3">
        <h2>My Reviews</h2>    

        <div class="row row-cols-1 row-cols-md-3 g-4">
            <% for (let i = 0; i < products.length; i++) { %>
                <div class="col">
                    <div class="card">
                        <div class="card-body">
                            <p class="card-text"><img src = "/images/<%= products[i].image %>" width="20%"></p>
                            <h5 class="card-title"><%= products[i].name %></a></h5>
                            <p class="card-text">Brand: <%= products[i].brand %></p>
                            <p class="card-text">Price: $<%= products[i].price %></p>
                            <p class="card-text">Rating: <%= products[i].rating %>/5 stars</p>
                            <p class="card-text">Category: <%= products[i].category %></p>
                            <div class="buttons">
                                <div class="d-flex justify-content-between">
                                    <a href="/updateReview/<%= products[i].productId %>" class="btn btn-warning">Edit</a>
                                    <a href="/productInfo/<%= products[i].productId %>" class="btn btn-primary">More Info</a>
                                    <a href="/deleteReview/<%= products[i].productId %>" class="btn btn-danger" onclick="return confirm('Are you sure you want to delete this review?')">Delete</a>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            <% } %>
        </div>

    </div>
</body>
</html>